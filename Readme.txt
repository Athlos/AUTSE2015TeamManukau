TO INSTALL DATABASE
please install heidiSQL
open it and make a new connection called "SE" with default settings (which is local host)
you want to click file > load SQL file and select the file in your github folder
it will create an SQL query to create the database, click the blue arrow or f9 to execute it and create the database
it has no data, just keys
use it to test input and output functionality